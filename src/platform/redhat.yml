- yum: 
    update_cache: true
    
- yum: 
    name: "{{item}}"
    state: latest
  with_items: 
    - yum-utils
    - epel-release
    - device-mapper-persistent-data
    - lvm2
    - bind-utils
    - moreutils
